# assets/

Placeholders for visual content the README links to — every file here is a plain SVG marked
"PLACEHOLDER", not a broken image link or a faked screenshot.

| File                                       | Replace with                                                                                               |
| ------------------------------------------ | ---------------------------------------------------------------------------------------------------------- |
| `logo-placeholder.svg`                     | Real FlowMind AI logo (square, transparent background recommended)                                         |
| `screenshot-health-placeholder.svg`        | Real screenshot of `GET /health`'s JSON response                                                           |
| `screenshot-workflow-runs-placeholder.svg` | Real screenshot of `GET /workflow-runs`'s JSON response, or a `jq`-formatted terminal view                 |
| `demo-placeholder.svg`                     | A real ~90s GIF/video of `pnpm demo` running — see [docs/demo/record-demo.md](../docs/demo/record-demo.md) |

Once the visual workflow editor ships (v0.3+), add real UI screenshots here too.
